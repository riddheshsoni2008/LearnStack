# LearnStack Backend Environment Variables
# Copy this file to .env and fill in your values

PORT=5000
NODE_ENV=development

# MongoDB Atlas Connection String
# Get this from: https://cloud.mongodb.com → Connect → Drivers → Node.js
MONGO_URI=mongodb+srv://riddheshsoni:<db_password>@riddheshsoni.smlgcdg.mongodb.net/

# JWT Secret (use any random string — keep it secret!)
JWT_SECRET=learnstack_super_secret_key_change_this_in_production

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
